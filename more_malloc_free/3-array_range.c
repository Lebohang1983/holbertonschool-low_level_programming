#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 *int *array_range: Creates an array of intergers
 *@min: all minimum values
 *@max: all maximum values
 * Return: pointer
 */
int *array_range(int min, int max)
{
	 int i;
	 int (*ptr)[];
	 unsigned int *str;

	for(int arr[]= i ,min <= i <= max, i++)
		return(ptr);
	str = malloc(sizeof(arr));
       	if (str == NULL && min > max)
	       	return (NULL);
}
