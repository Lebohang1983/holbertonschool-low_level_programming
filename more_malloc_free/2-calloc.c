#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 *void *_calloc- function that allocates memory for an array
 * nmemb: number of elements in the array to be allocated memory
 * @i: size of array
 */
void *_calloc(unsigned int nmemb, unsigned int size)
{
	int arr[nmemb];
	unsigned int i;
        int *ptr;
	int ctr;

	ctr = calloc(size(i) nmemb);
	ptr = malloc(size of(i));
	if {
		(nmemb = 0 && ptr == null)
		while ptr = 0 
	calloc == NULL
	}
	_putchar(ptr)
}
